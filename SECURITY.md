🔐 SECURITY.md - Password Manager Security Documentation

## Version: 1.1.0 (Security Enhanced Edition)

---

## ⚠️ DISCLAIMER

Это приложение предоставляется БЕЗ ГАРАНТИЙ. Используйте на свой риск.
**Используется в собственной системе, не в публичных компьютерах или облачных системах.**

---

## 🛡️ ЧТО ЗАЩИЩЕНО

### Криптография
- ✅ **Argon2id** для деривации ключей (вместо слабого PBKDF2)
  - Защита от brute-force и GPU-атак
  - Параметры: memory=65536, time=3, parallelism=4
  
- ✅ **Fernet (AES-128-CBC)** для шифрования хранилища
  - Шифрование + HMAC в одном
  
- ✅ **Шифрование паролей на лету**
  - Каждый пароль имеет свой Fernet-ключ
  - Генерируется случайно, хранится зашифрованным
  
- ✅ **HMAC-SHA256** для проверки целостности
  - Обнаружение любых модификаций vault.enc

### Управление памятью
- ✅ Пароли НЕ хранятся в памяти в открытом виде
  - Расшифровываются только при использовании
  - Очищаются сразу после копирования
  
- ✅ Затирание переменных
  - Удаление master_hash при lock
  - Очистка fernet-ключа
  
- ✅ Минимальное время жизни секретов
  - Auto-hide пароля через 5 секунд (GUI)
  - Очистка clipboard через 10 секунд

### Сессии и таймауты
- ✅ **Короткий SESSION_TIMEOUT** (120 сек / 2 минуты)
  - Вместо 5 минут по умолчанию
  
- ✅ **Auto-lock** при неактивности
  - Удаление всех ключей из памяти
  - Очистка clipboard
  
- ✅ **Обязательное повторное вводе пароля** после timeout

### Авторизация
- ✅ **Ограничение попыток входа** (3 максимум)
  - Exponential backoff: 1s, 2s, 4s
  
- ✅ **Минимальная длина master-password** (10 символов)
  - + требование заглавной, строчной буквы, цифры
  
- ✅ **Проверка целостности парольного хеша**
  - Сравнение Argon2 хеша перед разрешением доступа

### Защита от компрометации памяти
- ✅ **Обнаружение debugger** (sys.gettrace())
  - Выдает предупреждение при старте
  
- ✅ **Расшифровка по одной записи**
  - Весь vault не расшифровывается сразу
  
- ✅ **Очистка clipboard при lock**
  - Проверка, что это наш секрет перед очисткой

### Хранилище и backup
- ✅ **Атомные записи** (temp-файл + rename)
  - Предотвращение частичного перезаписания
  
- ✅ **Автоматические backups**
  - Сохраняются в ~/.password_manager/backups/
  - Хранятся 10 последних снимков
  
- ✅ **Версионирование формата**
  - _format_version в vault для миграций
  
- ✅ **Проверка целостности при загрузке**
  - HMAC-SHA256 перекрестная проверка

---

## ❌ ЧТО НЕ ЗАЩИЩЕНО

### Компьютерный уровень
- ❌ **RAT (Remote Access Trojan)** - может видеть все
  - Используется бесплатный, открытый Python
  - Нет стоп-кода при обнаружении RAT
  
- ❌ **Keylogger** - перехватит ввод пароля
  - Системные инструменты не могут защитить
  
- ❌ **Memory dump** - при компрометации прав администратора
  - Даже Argon2 не поможет если память скопирована
  
- ❌ **Physical attacks** - доступ к диску напрямую
  - Холодная загрузка, прямой доступ к flash
  
- ❌ **USB-брутфорс** - полный перебор master-password
  - Argon2id замедляет, но не исключает возможность

### Пользовательский уровень
- ❌ **Слабый master-password** - 10 символов можно подобрать
  - Используйте 20+ символов для реального использования
  
- ❌ **Переиспользование пароля** - приложение не проверяет
  - Используйте разные пароли для каждого сервиса
  
- ❌ **Фишинг** - введете пароль в поддельное приложение
  - Всегда запускайте из понятного вам каталога
  
- ❌ **Социнженерия** - расскажете пароль кому-то
  
- ❌ **Привычки** - если вы перезаписали пароль, старый может быть в памяти ОС

### Сетевые угрозы
- ❌ **Отправка паролей** - приложение НЕ отправляет ничего в интернет
  - Но если вы скопируете и отправите - ваша ответственность
  
- ❌ **Синхронизация** - нет встроенного облака
  - Backup-файлы могут быть украдены с диска

### Проблемы с форматом
- ❌ **Python source code** - может быть декомпилирован (.pyc)
- ❌ **PyInstaller бинарник** - может быть распакован
- ❌ **Отсутствие обфускации** - исходный код открыт для анализа

---

## 🔐 THREAT MODEL

### Предполагаемые угрозы (защищено)

| Угроза | Риск | Защита |
|--------|------|--------|
| Перебор master-password (offline) | ВЫСОКИЙ | Argon2id (1 сек/попытка) |
| Копирование vault.enc со диска | ВЫСОКИЙ | AES-128 + HMAC |
| Модификация vault.enc | ВЫСОКИЙ | HMAC-SHA256 проверка |
| Утечка пароля в памяти (snapshot) | СРЕДНИЙ | Расшифровка на лету |
| Утечка пароля в логах | СРЕДНИЙ | Нет логирования паролей |
| Долгая открытость хранилища | СРЕДНИЙ | Auto-lock (120 сек) |

### Невозможные для защиты

| Угроза | Риск | Почему? |
|--------|------|--------|
| RAT с root-доступом | КРИТИЧЕСКИЙ | Python код открыт, может перехватить все |
| Keylogger | КРИТИЧЕСКИЙ | Перехватит ввод пароля на уровне ОС |
| Memory dump с админ-правами | КРИТИЧЕСКИЙ | Вся памяль процесса скопирована |
| Физический доступ к диску | КРИТИЧЕСКИЙ | Можно читать весь диск без ОС |
| Компрометированная ОС | КРИТИЧЕСКИЙ | Ничего не поможет |

---

## 🔄 МИГРАЦИЯ С v1.0

Если у вас есть старое хранилище (PBKDF2):

1. Экспортируйте все пароли из v1.0:
   ```bash
   python password_manager.py
   # list -> вывести все
   # Ручное копирование
   ```

2. Удалите старые файлы:
   ```bash
   rm ~/.password_manager/vault.enc
   rm ~/.password_manager/salt
   ```

3. Запустите v1.1.0:
   ```bash
   python password_manager_secure.py
   # Создайте новый (БОЛЕЕ СИЛЬНЫЙ) master-password
   ```

4. Переимпортируйте пароли

---

## ✅ BEST PRACTICES

### Пароль
- 🔐 Используйте **20+ символов** (не 10)
- 🔐 Используйте **random** (не слово из словаря)
- 🔐 **Никому не говорите** и не записывайте
- 🔐 **Разные пароли** для разных сервисов

### Хранилище
- 📁 Регулярно проверяйте **backups/**
- 📁 **Синхронизируйте** на USB или облако (если вы его доверяете)
- 📁 **Не размещайте** на файлоделке / облаке без дополнительного шифрования
- 📁 Установите **800 permissions** (только владелец может читать)

```bash
chmod 700 ~/.password_manager
chmod 600 ~/.password_manager/*
```

### Использование
- 🖥️ Запускайте только на **собственном компьютере**
- 🖥️ Не открывайте на публичных ПК / интернет-кафе
- 🖥️ **Lock** после использования (не оставляйте открытым)
- 🖥️ Проверяйте **процессы** - нет ли подозрительных

### Резервное копирование
```bash
# Создать backup вручную
cp ~/.password_manager/vault.enc ~/backup_vault_$(date +%Y%m%d).enc

# Хранить в безопасном месте (USB, зашифрованный диск)
# НЕ облако без дополнительного шифрования!
```

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Алгоритмы

| Компонент | Алгоритм | Параметры |
|-----------|----------|-----------|
| Key derivation | Argon2id | memory=65536, time=3, parallelism=4 |
| Encryption | AES-128-CBC | Fernet (cryptography lib) |
| Integrity | HMAC-SHA256 | 32-byte key |
| Password verify | Argon2 verify | Compare hashes |
| Random data | os.urandom() | OS-provided CSPRNG |

### Файловая структура

```
~/.password_manager/
├── vault.enc          # Основное хранилище (зашифровано)
├── vault.hmac         # HMAC для проверки целостности
├── salt               # Salt для Argon2 (32 bytes)
├── hmac               # HMAC-key (32 bytes)
└── backups/           # Автоматические backups
    ├── vault_20250130_120000.enc
    └── vault_20250130_121500.enc
```

### Безопасность паролей в vault

Каждый пароль:
1. Шифруется своим Fernet-ключом (256-bit)
2. Ключ хранится в vault (тоже зашифрован основным Fernet)
3. Расшифровывается только при get()
4. Сразу затирается после использования (в теории)

```json
{
  "GitHub": {
    "username": "user@example.com",
    "password": "gAAAAABn... (зашифрованный пароль)",
    "password_key": "gAAAAABn... (зашифрованный ключ)",
    "notes": "2FA enabled",
    "modified": "2025-01-30T12:00:00"
  }
}
```

---

## 🚨 НАЙДЕННЫЕ УЯЗВИМОСТИ

Если вы нашли уязвимость:

1. **НЕ публикуйте** в GitHub issues
2. Напишите в email: [закрыто для приватности]
3. Описание и шаги воспроизведения
4. Дайте 30 дней на исправление

---

## 📊 AUDIT HISTORY

- **v1.1.0** (2025-01-30)
  - Переход на Argon2id
  - HMAC целостность
  - Шифрование на лету
  - Расширенные таймауты
  - Exponential backoff
  - Backup система
  - Auto-lock

- **v1.0.0** (2025-01-15)
  - Начальный release (PBKDF2 - слаба!)

---

## 📚 ССЫЛКИ

- [Argon2 RFC](https://github.com/P-H-C/phc-winner-argon2)
- [OWASP - Password Storage Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html)
- [Cryptography.io - Fernet](https://cryptography.io/en/latest/fernet/)
- [CWE-327: Use of a Broken or Risky Cryptographic Algorithm](https://cwe.mitre.org/data/definitions/327.html)

---

**Last updated:** 2025-01-30
**Status:** STABLE
